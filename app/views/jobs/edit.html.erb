<div class="sm:flex sm:items-center mb-10">
    <div class="sm:flex-auto">
        <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl"><%= t("views.jobs.edit_job") %></p>
    </div>
</div>
<%= render partial: "form" %>