<%= turbo_frame_tag "right_content" do %>
  <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Edit the mission order</p>
  <%= render partial: "form", locals: {user: @user, mission_order: @mission_order} %>
<% end %>
