<div class="border-gray-200 pb-5 sm:pb-0 mt-10">
  <div class="mt-3 sm:mt-4">
    <div class="hidden sm:block">
      <nav class="-mb-px flex space-x-8 user-submenu">
          <%= link_to t("views.jobs.view_job"), job_path(job_id), data: { turbo_frame: 'tabbed-content', turbo_action: 'advance'} %>
          <%= link_to t("views.jobs.job_applicants"), job_job_applications_path(job_id), data: { turbo_frame: 'tabbed-content', turbo_action: 'advance'} %>
      </nav>
    </div>
  </div>
</div>
