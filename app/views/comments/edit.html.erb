<%= turbo_frame_tag dom_id(@comment) do %>
  <%= render partial: "form", locals: {comment: @comment, commentable_type: @commentable_type, commentable_id: @commentable_id, author: @author, url: @url} %>
<% end %>
