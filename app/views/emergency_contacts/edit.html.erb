<div class="isolate bg-white px-6  lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Edit Emergency Contact</h2>
    </div>
</div>
<turbo-frame id="emergency_contact<%= @emergency_contact.id %>">
    <%= render "form", emergency_contact: @emergency_contact %>
</turbo-frame>
